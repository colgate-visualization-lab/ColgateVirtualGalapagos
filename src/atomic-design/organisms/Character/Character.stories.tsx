import React from "react";
import { CharacterProps, Character } from "./Character";
import { Story } from "@storybook/react";

export default {
  title: "Organisms/Character",
  // component: Character,
};

// const Template: Story<CharacterProps> = (args) => {
//   return <Character {...args} />;
// };

// export const Primary = Template.bind({});
// Primary.args = {
//     name:"adriana",
// };

