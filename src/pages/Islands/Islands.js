import React from "react";

import Map from "./Map";
const Islands = () => {
  return (
    <div
      style={{
        height: "90%",
        padding: "20px",
        width: "auto",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Map />
    </div>
  );
};

export default Islands;
